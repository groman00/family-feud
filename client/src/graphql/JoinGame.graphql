mutation JoinGame($token: String!, $playerName: String!) {
  joinGame(token: $token, playerName: $playerName) {
    game {
      token
      players {
        id
        name
      }      
    }
  }  
}
